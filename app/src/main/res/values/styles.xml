<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="android:Theme.Material.Light.DarkActionBar">
        <!-- Customize your theme here. -->
<!--        <item name="android:actionBarStyle">@style/PopularMovieActionBar</item>-->
    </style>

    <!-- Custom attributes for app theme-->
    <style name="PopularMovieActionBar" parent="android:Theme.Material.Light.DarkActionBar">
        <item name="android:background">@color/action_bar_color</item>
    </style>
    <style name="accent_button_style" parent="android:Widget.Button">
        <item name="android:textColor">#fff</item>
        <item name="android:padding">0dp</item>
        <item name="android:minWidth">88dp</item>
        <item name="android:minHeight">36dp</item>
        <item name="android:layout_margin">3dp</item>
        <item name="android:elevation">1dp</item>
        <item name="android:translationZ">1dp</item>
        <item name="android:background">@drawable/primary_round</item>
    </style>

    <!--app colors-->
    <color name="accent_color_button">#80CBC4</color>
    <color name="action_bar_color">#000</color>
    <color name="action_bar_overflow_color">#FFF</color>


</resources>
